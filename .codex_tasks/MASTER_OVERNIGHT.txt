TITLE: Overnight full enablement for Multi-Vendor B2B/B2C + In-App Agent (Supabase + App) — GPT-5

SCOPE (from “תכנון אפליקציה מולטי-וונדור” + “אפליקציית B2B מושלמת”):
- Multi-Vendor B2B:
  • entities: vendors, vendor_users, product_vendors, vendor_settings, payouts
  • strict tenant isolation (RLS by vendor_slug)
  • per-vendor catalog & SKU mapping; MOQ/min order rules
  • price lists per customer/group/vendor; tiered discounts
  • flows: Quick-Order (paste SKU/Qty), Quote→Order, Approvals, Budget/Credit limit, Returns/Credit notes
  • “My Store” (vendor dashboard): daily KPIs, top products, low stock, CSV export; payouts per period
- In-App AI Agent:
  • Agents SDK / Responses API
  • tools: search_products, price_for_customer, create_order, update_order_status, get_vendor_kpis (+ File Search if available)
  • guardrails (e.g., discount >X% needs approval)
  • web chat micro-frontend (Next.js) embedded in app (WebView/iframe), Hebrew copy
- B2C Playbook (Flatsome/WordPress) — docs only:
  • homepage sections: hot/new, WhatsApp CTA; no share buttons on product page
  • performance: LCP, critical CSS, fonts, caching; WP-CLI export/import; safe Staging→Prod

CONSTRAINTS:
- Non-destructive: no DROP; only CREATE IF NOT EXISTS / ALTER ADD
- Never commit secrets (.env/keys), update .gitignore
- Work in branch codex/overnight-{{today}}; many small commits; logs for each step
- Use Python 3.11 if present, else system python3; add eval_type_backport for py<3.10 typing

STEPS:

1) Repo & Env
- create/switch to branch codex/overnight-{{today}}
- .gitignore excludes: .env, .venv, node_modules, build, dist, logs, .dart_tool
- rebuild .venv; pip install -r requirements.txt + eval_type_backport
- ensure agent/__init__.py; make bin/run_agent.command run “python -m agent.run_agent”

2) Agent reliability (fallbacks)
- agent/order_agent.py: add “from __future__ import annotations”
- price_for_customer: if RPC /rest/v1/rpc/price_for_customer fails → read price from products (REST); return {"sku","customer_id","price","currency":"ILS"}
- create_order: if RPC fails → POST orders + order_items (REST), compute price from products; return {"order_id","customer_id","total","items"}
- agent/run_agent.py: add --once/--prompt for non-interactive smoke in Hebrew; friendly error messages

3) Supabase schema (minimal, non-destructive)
- create if missing:
  vendors(slug pk, name, status default 'active', created_at);
  vendor_users(vendor_slug fk, user_id text, role text default 'member', primary key(vendor_slug,user_id));
  product_vendors(sku text, vendor_slug text, primary key(sku,vendor_slug));
  vendor_settings(vendor_slug fk, settings jsonb, updated_at);
  payouts(id uuid pk, vendor_slug fk, period_start, period_end, amount numeric, currency text default 'ILS', status text default 'draft', created_at);
- indexes; enable RLS + base policies by vendor_slug
- seed lookups if needed: vendors('ashachar'), categories('uncategorized'), brands('generic')
- upsert demo product SP-NTM-32 price 12.90 slug sp-ntm-32 mapped to 'ashachar'

4) App (B2B “מושלם”)
- Quick-Order (paste SKU/Qty)
- Offline-first: cache lists/cart; queue writes for sync on reconnect
- “My Store” (vendor): daily KPIs, top products, low stock, CSV export
- Role-based UI (buyer/manager/vendor/admin); approvals UI for exceeding discount policy
- tests: repository/service with mocked Supabase; lint (dart analyze / npm test as applicable)

5) Admin Panel
- user management (invite/deactivate/reactivate) w/ audit
- Orders grid with filters (vendor/customer/status) + CSV export
- guardrail policy config (discounts > X% require approval) — infra + TODO hook

6) In-App AI Assistant
- ai/assistant/ (Python/Node tiny server) using Agents SDK/Responses API
- wire tools to Supabase REST: search_products, price_for_customer, create_order, update_order_status, get_vendor_kpis (+ File Search if vector store present)
- ai/chat-ui (Next.js) on port 3100; embed via WebView/iframe at /assistant
- README run locally; secrets server-side only

7) Evals (trace grading)
- docs/evals/orders_eval.yaml with ~10 Hebrew prompts (pricing/order/approval/stock)
- script to run; output logs/evals-{{timestamp}}.md
- iterate prompt if fails; commit summary

8) B2C Playbook (docs only)
- docs/wp_b2c_playbook.md: homepage hot/new, WhatsApp CTA, no product share buttons, perf (LCP/CSS/fonts/cache), WP-CLI snippets, Staging→Prod

9) Cleanup & Tooling
- remove dead files; pre-commit (black/isort/ruff or relevant linters; dart format)
- secret scan; ensure .env ignored
- feature flags if present

10) Smoke + Summary
- python -m agent.run_agent --once --prompt "פתח הזמנה ל-5 יחידות של SP-NTM-32 ללקוח 123."
- docs/overnight_summary.md: what changed, TODO board (priority/owner placeholders)
- all step logs into logs/*.log

NOTES:
- OPENAI_API_KEY from environment only; never log keys
- If FKs block seed: upsert lookups first then retry
- Hebrew UX copy where applicable; concise code comments
