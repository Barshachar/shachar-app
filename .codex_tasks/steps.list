Repo & Env: create .gitignore (.env,.venv,node_modules,build,dist,logs,.dart_tool); rebuild .venv; pip install -r requirements.txt + eval_type_backport; ensure agent/__init__.py; make bin/run_agent.command run "python -m agent.run_agent".
Agent: add "from __future__ import annotations" to agent/order_agent.py; add fallback price_for_customer (read price from products via REST); add fallback create_order (insert orders+order_items via REST, price from products); make agent/run_agent.py support --once --prompt (Hebrew errors).
Supabase schema: create-if-missing vendors/vendor_users/product_vendors/vendor_settings(jsonb)/payouts; add indexes; enable RLS with base policies by vendor_slug; seed vendors('ashachar')/categories('uncategorized')/brands('generic'); upsert product SP-NTM-32 price 12.90 slug sp-ntm-32 linked to 'ashachar'.
B2B App: add Quick-Order (paste SKU,Qty); add offline-first (cache lists/cart, queue writes, sync on reconnect); add "My Store" (KPIs, top products, low stock, CSV); add role-based UI (buyer/manager/vendor/admin) + approvals UI for discount > X%.
Admin: ensure user management (invite/deactivate/reactivate) with audit; add Orders grid with filters (vendor/customer/status) + CSV export; add guardrail config doc/hook (discounts > X% require approval).
In-App Agent: create ai/assistant server (Agents SDK/Responses) with tools search_products, price_for_customer, create_order, update_order_status, get_vendor_kpis (+ File Search if vector store); create ai/chat-ui (Next.js on 3100) and document embedding as WebView/iframe at /assistant; README; secrets server-side only.
Evals: create docs/evals/orders_eval.yaml (~10 Hebrew prompts); add run script; write logs/evals-{{timestamp}}.md; refine prompt/guardrails if failures; commit summary.
B2C Playbook (docs only): create docs/wp_b2c_playbook.md (homepage hot/new, WhatsApp CTA, remove product share; perf LCP/Critical CSS/Fonts/Cache; WP-CLI export/import; safe Staging→Prod).
Cleanup & Tooling: remove dead files; add pre-commit (black/isort/ruff or relevant; dart format); secret scan; ensure .env ignored; feature flags if exist.
Smoke + Summary: run "python -m agent.run_agent --once --prompt 'פתח הזמנה ל-5 יחידות של SP-NTM-32 ללקוח 123.'"; write docs/overnight_summary.md (what changed + TODO board); keep logs under logs/*.
