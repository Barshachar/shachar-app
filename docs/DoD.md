# Definition of Done

- Supabase schema and policies deploy without errors (`supabase db reset`).
- RLS negative tests in `policies.sql` pass.
- Flutter analyze + tests pass (mobile + web).
- Edge function deno tests pass.
- `POST_BUILD_VALIDATION.md` checklist executed and signed off.
- Docs (`ERD`, `RLS matrix`, `API`, ADRs, runbooks) updated when features change.
- Seeds refreshed to keep demo accounts valid.
- CI workflow green with artifacts (web bundle + APK produced locally via scripts).
