name: offline_toolkit
description: Offline-first primitives (cache, queue, sync) shared across א.שחר apps.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  drift: ^2.14.0
  drift_flutter: ^0.2.6
  sqlite3_flutter_libs: ^0.5.0
  workmanager: ^0.9.0+3
  connectivity_plus: ^7.0.0
  logging: ^1.2.0
  collection: ^1.18.0
  supabase_flutter: ^2.5.4
  riverpod: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
